model:
  family: ultralytics
  version: "12"
  task: detect
  backend: onnx
  path: /home/bdi/models/yolo12n.onnx
  imgsz: 640
  class_names: coco
  meta: /home/bdi/models/yolo12n.onnx.meta.yaml
  status: experimental
  note: "YOLO12 support is experimental/research-oriented. Verify upstream Ultralytics runtime compatibility before deployment."

inference:
  conf: 0.25
  iou: 0.45
  classes: []
  max_det: 300

external:
  yolov5_repo: ""

ros:
  image_topic: /camera/color/image_raw
  camera_info_topic: /camera/color/camera_info
  detections_topic: /yolo/detections
  overlay_topic: /yolo/overlay
  publish_overlay: true
  queue_size: 1

